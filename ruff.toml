# Мягкий линтер: ловим реальные ошибки (синтаксис, неопределённые имена),
# стиль не навязываем — кодовая база исторически без форматтера.
line-length = 100

[lint]
select = ["E9", "F63", "F7", "F82"]
